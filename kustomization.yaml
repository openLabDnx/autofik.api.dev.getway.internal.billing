apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# secret.yml is deliberately not listed - it holds real API keys, is
# gitignored, and is applied separately (make secret). Everything here is
# safe to commit.
resources:
  - billing-getway.yml
